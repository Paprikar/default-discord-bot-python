from datetime import time
from typing import List


def get_pics_path_list(directory: str) -> List[str]:
    """Returns the list of pictures in the directory.

    Note:
        Only images with the following extensions are taken into account:
        ['.png', '.jpg', '.jpeg'].

    Returns:
        The list of image paths.

    """


def codepoint_to_str(cp: str) -> str:
    """Converts a string of the 'U+XXXX' format
    into a corresponding Unicode character.

    Returns:
        Converted Unicode character.

    Raises:
        ValueError: Error at conversion.

    """


def time_in_range(start: time, end: time, x: time) -> bool:
    """Checks if ``x`` is in the range [``start``, ``end``).

    Returns:
        Result of the check.

    """
